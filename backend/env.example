# Medical AI Backend - Environment Variables Example
# Copy this file to .env and fill in your actual values
# For Railway deployment, set these as environment variables in the Railway dashboard

# ============================================
# OpenAI Configuration (REQUIRED)
# ============================================
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_DEFAULT_MODEL=gpt-4o-mini
# Alternative: DEFAULT_ANALYSIS_MODEL=gpt-4o-mini
# Vision model for image analysis (optional):
# VISION_ANALYSIS_MODEL=gpt-4o
# FALLBACK_CHAT_MODEL=gpt-4o-mini

# ============================================
# Supabase Configuration (REQUIRED for document storage)
# ============================================
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_KEY=your_supabase_service_role_key_here
SUPABASE_BUCKET=patient-documents
# Note: Create the bucket in Supabase Storage before deploying

# ============================================
# SMTP Configuration (OPTIONAL - for email notifications)
# ============================================
# If not configured, email features will be disabled
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your_email@gmail.com
SMTP_PASSWORD=your_app_password_here
SMTP_FROM=your_email@gmail.com
SMTP_USE_TLS=true

# ============================================
# CORS Configuration (OPTIONAL)
# ============================================
# Comma-separated list of allowed origins (e.g., "https://example.com,https://app.example.com")
# If not set, defaults to localhost and Railway domains
CORS_ORIGINS=
# Regex pattern for additional origin matching (optional)
# Default includes Railway domains: https://.*\.railway\.app|https://.*\.railway\.dev
CORS_ORIGIN_REGEX=

# ============================================
# OCR Configuration (OPTIONAL)
# ============================================
# Only needed if you want OCR functionality for scanned PDFs
# OCR_ZOOM=3.0
# OCR_PSM=6
# TESSERACT_CMD=C:\Program Files\Tesseract-OCR\tesseract.exe  # Windows only

# ============================================
# Server Configuration
# ============================================
# PORT is automatically set by Railway, don't override it

